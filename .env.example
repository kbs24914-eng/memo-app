# Local: SQLite (optional) or Neon PostgreSQL connection string
# Vercel: set DATABASE_URL in project Environment Variables (Neon integration)
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"
AUTH_SECRET="your-random-secret-at-least-32-chars"
